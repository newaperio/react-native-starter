const { colors } = require("./colors")

module.exports = {
  theme: {
    colors: colors,
    fontFamily: {
      body: ["DM Sans", "sans-serif"],
      heading: ["Red Hat Display", "sans-serif"],
    },
  },
}
